<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <!--
        Fix 10: CALL_PHONE removed.
        StoneGuard never makes phone calls. This permission had no legitimate
        use in the app and would trigger a Play Store sensitive-permissions
        review.
    -->

    <!--
        Required alongside internet permission so app works on
        tablets/devices without a SIM card (telephony feature is optional).
    -->
    <uses-feature android:name="android.hardware.telephony" android:required="false"/>

    <!--
        allowBackup="false"        : prevents Android system backup from copying
                                     encrypted DB/prefs to Google Drive or adb.
        fullBackupContent="false"  : belt-and-suspenders for pre-API-23 devices.
        dataExtractionRules        : API 31+ (Android 12) backup/transfer policy.
                                     See res/xml/data_extraction_rules.xml.
        networkSecurityConfig      : enforces HTTPS-only; blocks all cleartext HTTP.

        Batch B: AdMob App ID is NO LONGER hardcoded here.
        The ${admobAppId} placeholder is injected at build time from
        android/local.properties (which is gitignored).
        Add this line to android/local.properties:
            admobAppId=ca-app-pub-XXXXXXXXXXXXXXXX~XXXXXXXXXX
        And add this to android/app/build.gradle.kts defaultConfig block:
            manifestPlaceholders["admobAppId"] = localProperties.getProperty("admobAppId", "ca-app-pub-3940256099942544~3347511713")
        The fallback value above is Google's official test App ID — safe for
        development builds when local.properties is absent (e.g. CI).
    -->
    <application
        android:label="StoneGuard"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- AdMob App ID — injected from local.properties via manifestPlaceholders -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="${admobAppId}"/>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <receiver android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false"/>
        <receiver android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>

    </application>

</manifest>
