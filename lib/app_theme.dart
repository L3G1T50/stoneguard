// ─── BACKWARD-COMPAT SHIM ────────────────────────────────────────────────────
// Screens that still use `import '../app_theme.dart'` will get the same
// colors and styles from the canonical theme/app_theme.dart file.
export 'theme/app_theme.dart';
